[pytest]
testpaths = tests
pythonpath = . src
addopts = -ra
# Fixtures are repositories-under-test, NOT pytest test modules.
# `norecursedirs` skips subtrees during test discovery.
norecursedirs = tests/runtime/fixtures