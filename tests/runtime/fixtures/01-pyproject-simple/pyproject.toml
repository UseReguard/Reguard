[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simple-pkg"
version = "0.1.0"
description = "Simple pyproject fixture"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["simple_pkg"]
