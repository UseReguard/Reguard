"""Curated GitHub corpus of Python AI-agent repositories.

Stores metadata + URLs in `agent_repositories`. Never clones or stores
source code here — that work belongs in the `pipeline` domain.

CLI:
    python -m compliance.corpus discover|stats|list|refresh|reclassify
"""